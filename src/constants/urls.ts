//TODO: the base url must obtain form env variables but for simplicity this works (note that this not base url at all!!)

const v3 = "v3";

export const BASE_URL = "https://restcountries.com";

export const countriesUrl = `${BASE_URL}/${v3}/all`;
